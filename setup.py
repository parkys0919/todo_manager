from setuptools import setup, find_packages

setup(
    name="todo_manager",
    version="1.0.0",
    author="Park Yunseung",
    description="A Python package for managing tasks and recurring tasks",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)